// This file is no longer needed as MUI has been completely removed
// Keeping it as an empty module to prevent import errors during transition

const emptyFunction = () => null;

export const createStyledComponent = emptyFunction;
export const styled = emptyFunction;
